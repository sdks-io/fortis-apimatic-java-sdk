/*
 * FortisApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package tech.fortis.sandbox.api.controllers;

import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;
import tech.fortis.sandbox.api.ApiHelper;
import tech.fortis.sandbox.api.Server;
import tech.fortis.sandbox.api.exceptions.ApiException;
import tech.fortis.sandbox.api.exceptions.Response401TokenException;
import tech.fortis.sandbox.api.exceptions.Response412Exception;
import tech.fortis.sandbox.api.http.request.HttpMethod;
import tech.fortis.sandbox.api.http.response.ApiResponse;
import tech.fortis.sandbox.api.models.Expand60;
import tech.fortis.sandbox.api.models.ResponseTransaction;
import tech.fortis.sandbox.api.models.V1TransactionsAuthCompleteRequest;
import tech.fortis.sandbox.api.models.V1TransactionsAuthIncrementRequest;
import tech.fortis.sandbox.api.models.V1TransactionsPartialReversalRequest;
import tech.fortis.sandbox.api.models.V1TransactionsRefundRequest;
import tech.fortis.sandbox.api.models.V1TransactionsTipAdjustRequest;
import tech.fortis.sandbox.api.models.V1TransactionsVoidRequest;

/**
 * This class lists all the endpoints of the groups.
 */
public final class TransactionsUpdatesController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public TransactionsUpdatesController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Void a transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ResponseTransaction> void_(
            final String transactionId,
            final List<Expand60> expand) throws ApiException, IOException {
        return prepareVoid_Request(transactionId, expand).execute();
    }

    /**
     * Void a transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ResponseTransaction>> voidAsync(
            final String transactionId,
            final List<Expand60> expand) {
        try {
            return prepareVoid_Request(transactionId, expand).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for void_.
     */
    private ApiCall<ApiResponse<ResponseTransaction>, ApiException> prepareVoid_Request(
            final String transactionId,
            final List<Expand60> expand) {
        return new ApiCall.Builder<ApiResponse<ResponseTransaction>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v1/transactions/{transaction_id}/void")
                        .queryParam(param -> param.key("expand")
                                .value(Expand60.toValue(expand)).isRequired(false))
                        .templateParam(param -> param.key("transaction_id").value(transactionId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("user-id")
                                        .add("user-api-key")
                                        .add("developer-id")))
                        .httpMethod(HttpMethod.PUT))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ResponseTransaction.class))
                        .nullify404(false)
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new Response401TokenException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Void a transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ResponseTransaction> patchVoid(
            final String transactionId,
            final V1TransactionsVoidRequest body,
            final List<Expand60> expand) throws ApiException, IOException {
        return preparePatchVoidRequest(transactionId, body, expand).execute();
    }

    /**
     * Void a transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ResponseTransaction>> patchVoidAsync(
            final String transactionId,
            final V1TransactionsVoidRequest body,
            final List<Expand60> expand) {
        try {
            return preparePatchVoidRequest(transactionId, body, expand).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for patchVoid.
     */
    private ApiCall<ApiResponse<ResponseTransaction>, ApiException> preparePatchVoidRequest(
            final String transactionId,
            final V1TransactionsVoidRequest body,
            final List<Expand60> expand) {
        return new ApiCall.Builder<ApiResponse<ResponseTransaction>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v1/transactions/{transaction_id}/void")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .queryParam(param -> param.key("expand")
                                .value(Expand60.toValue(expand)).isRequired(false))
                        .templateParam(param -> param.key("transaction_id").value(transactionId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("user-id")
                                        .add("user-api-key")
                                        .add("developer-id")))
                        .httpMethod(HttpMethod.PATCH))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ResponseTransaction.class))
                        .nullify404(false)
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new Response401TokenException(reason, context)))
                        .localErrorCase("412",
                                 ErrorCase.setReason("Precondition Failed",
                                (reason, context) -> new Response412Exception(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Perform an auth complete transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ResponseTransaction> authComplete(
            final String transactionId,
            final V1TransactionsAuthCompleteRequest body,
            final List<Expand60> expand) throws ApiException, IOException {
        return prepareAuthCompleteRequest(transactionId, body, expand).execute();
    }

    /**
     * Perform an auth complete transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ResponseTransaction>> authCompleteAsync(
            final String transactionId,
            final V1TransactionsAuthCompleteRequest body,
            final List<Expand60> expand) {
        try {
            return prepareAuthCompleteRequest(transactionId, body, expand).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for authComplete.
     */
    private ApiCall<ApiResponse<ResponseTransaction>, ApiException> prepareAuthCompleteRequest(
            final String transactionId,
            final V1TransactionsAuthCompleteRequest body,
            final List<Expand60> expand) {
        return new ApiCall.Builder<ApiResponse<ResponseTransaction>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v1/transactions/{transaction_id}/auth-complete")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .queryParam(param -> param.key("expand")
                                .value(Expand60.toValue(expand)).isRequired(false))
                        .templateParam(param -> param.key("transaction_id").value(transactionId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("user-id")
                                        .add("user-api-key")
                                        .add("developer-id")))
                        .httpMethod(HttpMethod.PATCH))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ResponseTransaction.class))
                        .nullify404(false)
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new Response401TokenException(reason, context)))
                        .localErrorCase("412",
                                 ErrorCase.setReason("Precondition Failed",
                                (reason, context) -> new Response412Exception(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Perform an auth increment transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ResponseTransaction> authIncrement(
            final String transactionId,
            final V1TransactionsAuthIncrementRequest body,
            final List<Expand60> expand) throws ApiException, IOException {
        return prepareAuthIncrementRequest(transactionId, body, expand).execute();
    }

    /**
     * Perform an auth increment transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ResponseTransaction>> authIncrementAsync(
            final String transactionId,
            final V1TransactionsAuthIncrementRequest body,
            final List<Expand60> expand) {
        try {
            return prepareAuthIncrementRequest(transactionId, body, expand).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for authIncrement.
     */
    private ApiCall<ApiResponse<ResponseTransaction>, ApiException> prepareAuthIncrementRequest(
            final String transactionId,
            final V1TransactionsAuthIncrementRequest body,
            final List<Expand60> expand) {
        return new ApiCall.Builder<ApiResponse<ResponseTransaction>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v1/transactions/{transaction_id}/auth-increment")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .queryParam(param -> param.key("expand")
                                .value(Expand60.toValue(expand)).isRequired(false))
                        .templateParam(param -> param.key("transaction_id").value(transactionId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("user-id")
                                        .add("user-api-key")
                                        .add("developer-id")))
                        .httpMethod(HttpMethod.PATCH))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ResponseTransaction.class))
                        .nullify404(false)
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new Response401TokenException(reason, context)))
                        .localErrorCase("412",
                                 ErrorCase.setReason("Precondition Failed",
                                (reason, context) -> new Response412Exception(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Perform a partial reversal.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ResponseTransaction> partialReversal(
            final String transactionId,
            final V1TransactionsPartialReversalRequest body,
            final List<Expand60> expand) throws ApiException, IOException {
        return preparePartialReversalRequest(transactionId, body, expand).execute();
    }

    /**
     * Perform a partial reversal.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ResponseTransaction>> partialReversalAsync(
            final String transactionId,
            final V1TransactionsPartialReversalRequest body,
            final List<Expand60> expand) {
        try {
            return preparePartialReversalRequest(transactionId, body, expand).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for partialReversal.
     */
    private ApiCall<ApiResponse<ResponseTransaction>, ApiException> preparePartialReversalRequest(
            final String transactionId,
            final V1TransactionsPartialReversalRequest body,
            final List<Expand60> expand) {
        return new ApiCall.Builder<ApiResponse<ResponseTransaction>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v1/transactions/{transaction_id}/partial-reversal")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .queryParam(param -> param.key("expand")
                                .value(Expand60.toValue(expand)).isRequired(false))
                        .templateParam(param -> param.key("transaction_id").value(transactionId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("user-id")
                                        .add("user-api-key")
                                        .add("developer-id")))
                        .httpMethod(HttpMethod.PATCH))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ResponseTransaction.class))
                        .nullify404(false)
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new Response401TokenException(reason, context)))
                        .localErrorCase("412",
                                 ErrorCase.setReason("Precondition Failed",
                                (reason, context) -> new Response412Exception(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Perform a refund transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ResponseTransaction> refundTransaction(
            final String transactionId,
            final V1TransactionsRefundRequest body,
            final List<Expand60> expand) throws ApiException, IOException {
        return prepareRefundTransactionRequest(transactionId, body, expand).execute();
    }

    /**
     * Perform a refund transaction.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ResponseTransaction>> refundTransactionAsync(
            final String transactionId,
            final V1TransactionsRefundRequest body,
            final List<Expand60> expand) {
        try {
            return prepareRefundTransactionRequest(transactionId, body, expand).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for refundTransaction.
     */
    private ApiCall<ApiResponse<ResponseTransaction>, ApiException> prepareRefundTransactionRequest(
            final String transactionId,
            final V1TransactionsRefundRequest body,
            final List<Expand60> expand) {
        return new ApiCall.Builder<ApiResponse<ResponseTransaction>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v1/transactions/{transaction_id}/refund")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .queryParam(param -> param.key("expand")
                                .value(Expand60.toValue(expand)).isRequired(false))
                        .templateParam(param -> param.key("transaction_id").value(transactionId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("user-id")
                                        .add("user-api-key")
                                        .add("developer-id")))
                        .httpMethod(HttpMethod.PATCH))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ResponseTransaction.class))
                        .nullify404(false)
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new Response401TokenException(reason, context)))
                        .localErrorCase("412",
                                 ErrorCase.setReason("Precondition Failed",
                                (reason, context) -> new Response412Exception(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Increment the authorized transaction amount to include a tip.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ResponseTransaction> tipAdjustment(
            final String transactionId,
            final V1TransactionsTipAdjustRequest body,
            final List<Expand60> expand) throws ApiException, IOException {
        return prepareTipAdjustmentRequest(transactionId, body, expand).execute();
    }

    /**
     * Increment the authorized transaction amount to include a tip.
     * @param  transactionId  Required parameter: Transaction ID
     * @param  body  Required parameter:
     * @param  expand  Optional parameter: Most endpoints in the API have a way to retrieve extra
     *         data related to the current record being retrieved. For example, if the API request
     *         is for the accountvaults endpoint, and the end user also needs to know which contact
     *         the token belongs to, this data can be returned in the accountvaults endpoint
     *         request.
     * @return    Returns the ResponseTransaction wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ResponseTransaction>> tipAdjustmentAsync(
            final String transactionId,
            final V1TransactionsTipAdjustRequest body,
            final List<Expand60> expand) {
        try {
            return prepareTipAdjustmentRequest(transactionId, body, expand).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for tipAdjustment.
     */
    private ApiCall<ApiResponse<ResponseTransaction>, ApiException> prepareTipAdjustmentRequest(
            final String transactionId,
            final V1TransactionsTipAdjustRequest body,
            final List<Expand60> expand) {
        return new ApiCall.Builder<ApiResponse<ResponseTransaction>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v1/transactions/{transaction_id}/tip-adjust")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .queryParam(param -> param.key("expand")
                                .value(Expand60.toValue(expand)).isRequired(false))
                        .templateParam(param -> param.key("transaction_id").value(transactionId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .and(andAuth -> andAuth
                                        .add("user-id")
                                        .add("user-api-key")
                                        .add("developer-id")))
                        .httpMethod(HttpMethod.PATCH))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ResponseTransaction.class))
                        .nullify404(false)
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new Response401TokenException(reason, context)))
                        .localErrorCase("412",
                                 ErrorCase.setReason("Precondition Failed",
                                (reason, context) -> new Response412Exception(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}